# Synthetic Ambassador Data Generator

Generates the four tables (`ambassador`, `ambassador_team`, `order_fact`, `signal_fact`) as Parquet, lands them in S3, and registers them as Iceberg in Glue. All PII is synthetic.

Stub. See `docs/repo-bootstrap-brief.md` §8 for schemas, volumes, and distribution rules.
